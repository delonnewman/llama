#+title: Notes

* Base
** Class
*** Enum
*** Union
*** Record
*** Role

* Type
** Protocol
- Signature
  - Value::Type
- Attribute
  - Attribute::Type
    - Value::Type
** Value::Type
** Attribute::Type

* Collections
** Enumerable (Role)
** Associative (Role)
** Pair
** Cons
** List
** LazyList
** Dictionary

* Base::Hash
** Entity (mutable heterogenous data)
** Record (immutable heterogenous data)
** Dictionary is Enumerable (mutable homogenous key/value lookup)

* Base::Array
** Vector is Enumerable (immutable indexed)

* Macros
** Attributes
** Delegation

* Prelude

* TODO Make Enum::Member and Union::Member subclasses of Class *(perhaps use Union::Member as the class type for Enum::Member)
** Override eigen_class should just be an alias for class
** Add convienices for using class attributes as meta data

* References
** Types & Contracts
https://theweeklychallenge.org/blog/unary-operator/
https://blogs.perl.org/users/leon_timmermans/2025/02/a-deep-dive-into-the-perl-type-systems.html
https://docs.racket-lang.org/reference/contracts.html
https://docs.racket-lang.org/guide/contract-boundaries.html
https://en.wikipedia.org/wiki/Comparison_of_programming_languages_(algebraic_data_type)

** Concurrency
http://cml.cs.uchicago.edu
https://wingolog.org/archives/2017/06/29/a-new-concurrent-ml
https://en.wikipedia.org/wiki/Concurrent_ML

** NREPL
https://github.com/nrepl/neat
https://nrepl.org/nrepl/building_servers.html

* Class
** Class::Sum
** Class::Product
** ::or -> Class::Sum (Union, Enum)
** ::and -> Class::Product (Tuple, Record)

* Base
** Base::Symbol (stateless, singleton)
** Base::Scalar
** Base::Array
** Base::Hash

* Enumerable
Look at Elixirs protocol and Clojure's Transducers.

https://elixir.hexdocs.pm/Enumerable.html
https://elixir.hexdocs.pm/enumerable-and-streams.html
https://clojure.org/reference/transducers
https://github.com/delonnewman/transducers-ruby
https://www.npmjs.com/package/@thi.ng/transducers

** Range
- Lazy range Enumerable, built in integration with native Perl iteration operators
- Protocol for iterating on objects

* Time Classes
- Scalar objects
- Integrate with Time::Piece optionally (DateTime)
- Integrate with Range

** Date
Iterates by day
** Time (Time of Day)
Iterates by 30 second intervals
** DateTime
Iterates by day
** Duration
Iterates by 30 second intervals

* Lisp
A Simple Lisp reader for Perl code. Put another way, a Lisp with Perl Semantics.
To simplify semantics will assume the following:
  - use strict 'vars'
  - Won't include syntax types that would be redundant for a Lisp

** Syntax
- Syntax Objects
  - VariableDefinition
    - LexicalVariableDefinition (my)
    - TransientVariableDefinition (state)
    - ExtendedGlobalVariableDefinition (local)
    - ScopedGlobalVariableDefinition (our)
  - Variable
    - Scalar
    - Array
    - Hash
    - Code
    - Glob
  - Control
    - Do
    - While
      - Until
    - DoWhile
      - DoUntil
    - ForEach
    - If (If/Elsif/Else)
      - Unless
    - Given/When
    - Sub
  - Operator
    - Deref
      - ArrayDeref (@$)
      - HashDeref (%$)
      - ScalarDeref ($$)
      - ParameterizedDeref
        - ParameterizedCodeDeref (->())
        - ParameterizedArrayDeref (->[])
        - ParameterizedHashDeref (->{})
    - Ref (\)
    - Binary
    - Unary
  - Data
    - List (same semantics as Perl, just that quoted lists evaluate to an ArrayRef)
    - String (quoted symbols are strings and symbols are just bare strings)
    - Number
    - Ref
      - ArrayRef ([...])
      - HashRef ({...})
      - CodeRef (sub{...})
